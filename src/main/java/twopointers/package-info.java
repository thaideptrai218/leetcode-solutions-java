/**
 * Two Pointers.
 *
 * <p>Variants:
 * <ul>
 *   <li>Opposite ends — sorted array sum, palindrome, container with most water. Move the side that improves the answer.</li>
 *   <li>Same direction (slow/fast) — remove duplicates in-place, partition; slow tracks write position.</li>
 *   <li>Sliding window — fixed or variable size; expand right, shrink left when constraint breaks.</li>
 *   <li>Cycle detection — Floyd's tortoise & hare on linked lists or sequences.</li>
 * </ul>
 *
 * <p>When you see "sorted array" or "in-place" or "subarray with property", reach for two pointers first.
 *
 * <p>Classic problems: 11, 15, 16, 26, 27, 42, 75, 88, 125, 167, 209, 283, 344, 567, 633, 977.
 */
package twopointers;
