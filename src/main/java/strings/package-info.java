/**
 * Strings.
 *
 * <p>Key patterns:
 * <ul>
 *   <li>Sliding window — substrings with constraint (longest without repeat, min window).</li>
 *   <li>Two pointers — palindrome check, reverse, compare from ends.</li>
 *   <li>Frequency map / int[26] — anagrams, character counts.</li>
 *   <li>StringBuilder — never concatenate in a loop; String is immutable.</li>
 *   <li>KMP / Rabin-Karp — pattern matching when naive O(n*m) is too slow.</li>
 * </ul>
 *
 * <p>Classic problems: 3, 5, 14, 28, 76, 125, 242, 438, 567.
 */
package strings;
